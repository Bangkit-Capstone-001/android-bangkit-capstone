<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.CapstoneApp" parent="Theme.Material3.Light.NoActionBar" />

    <style name="Theme.CapstoneApp" parent="Base.Theme.CapstoneApp">
        <!-- Secondary brand color. -->
        <item name="colorPrimary">@color/darkBlue</item>
        <item name="colorPrimaryVariant">@color/mediumBlue</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/contrastYellow</item>
        <item name="colorSecondaryVariant">@color/paleYellow</item>
        <item name="colorOnSecondary">@color/black</item>
    </style>

    <!--  NavBar: Active Indicator  -->
    <style name="App.Custom.Indicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">@color/mediumBlue</item>
    </style>

    <!--  GroupButton  -->
    <style name="ToggleButtonWithIconOnly" parent="Widget.MaterialComponents.Button.OutlinedButton">
        <item name="iconPadding">0dp</item>
        <item name="android:insetTop">0dp</item>
        <item name="android:insetBottom">0dp</item>
        <item name="android:paddingLeft">12dp</item>
        <item name="android:paddingRight">12dp</item>
        <item name="android:minWidth">48dp</item>
        <item name="android:minHeight">48dp</item>
        <item name="backgroundTint">@color/button_selector</item>
        <item name="rippleColor">@color/button_selector</item>
        <item name="strokeColor">@color/mediumBlue</item>
        <item name="android:textColor">@color/mediumBlue</item>
    </style>

    <!--  DatePicker  -->
    <style name="CustomDatePicker" parent="ThemeOverlay.MaterialComponents.MaterialCalendar">
        <item name="materialCalendarStyle">@style/CustomMaterialCalendar</item>
    </style>

    <style name="CustomMaterialCalendar" parent="Widget.MaterialComponents.MaterialCalendar">
        <item name="backgroundTint">@color/greyedWhite</item>
    </style>

</resources>